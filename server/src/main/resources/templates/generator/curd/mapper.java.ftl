package ${package.MAPPER};

import ${package.ENTITY}.${fileNameInfo.ENTITY};
import ${superMapperClassPackage};

/**
 * <p>
 * ${table.tableComment!} Mapper 接口
 * </p>
 *
 * @author ${table.codeAuthor}
 * @since ${date}
 */
public interface ${fileNameInfo.MAPPER} extends ${superMapperClass}<${fileNameInfo.ENTITY}> {

}
